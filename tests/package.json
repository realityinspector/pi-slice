{
  "name": "@slice/tests",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'no build needed'",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}

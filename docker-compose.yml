services:
  slice:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - slice-data:/data
    env_file:
      - .env
    environment:
      - PORT=8080
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  slice-data:
